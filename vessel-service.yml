server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/navy_vessel_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
