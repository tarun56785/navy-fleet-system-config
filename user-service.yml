server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/navy_user_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
application:
  security:
    jwt:
      secret-key: kjZkiL/+bIpdAeYYYrMQCRh0yb+09i/hK23P91sQRBI=
      expiration: 86400000 # 1 day in milliseconds
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8888/eureka/      
